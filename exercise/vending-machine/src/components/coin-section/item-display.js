import React, { Component } from "react";
import PropTypes from "prop-types";

/**
 * For displaying selecing item
 *
 * @typedef {object} Props
 *
 * @extends {Component<Props>}
 */
export default class ItemDisplay extends Component {
  static propTypes = {
    prop: PropTypes,
  };

  render() {
    return <div></div>;
  }
}
